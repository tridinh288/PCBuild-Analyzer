# PCBuild Analyzer — Frontend

React + Vite + Tailwind. See the [root README](../README.md) for setup.

```bash
cp .env.example .env
npm install
npm run dev      # http://localhost:5173
npm test         # unit tests (Vitest)
```
