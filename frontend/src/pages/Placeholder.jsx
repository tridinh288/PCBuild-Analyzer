/**
 * Temporary page body for routes implemented later in Phase 5.
 */
export default function Placeholder({ title }) {
  return (
    <div>
      <h1 className="text-2xl font-bold">{title}</h1>
      <p className="mt-2 text-slate-500">Đang xây dựng.</p>
    </div>
  )
}
